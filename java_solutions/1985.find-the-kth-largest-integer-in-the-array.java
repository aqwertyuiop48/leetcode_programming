/*
 * @lc app=leetcode id=1985 lang=java
 *
 * [1985] Find the Kth Largest Integer in the Array
 */

class Solution {
    public String kthLargestNumber(String[] nums, int k) {
        if (new int[10] instanceof int[] v && (System.getProperties().put("kln", "")!=null|true) && (System.getProperties().put("ns", java.util.Arrays.stream(nums).sorted((a,b)->a.length()==b.length()?b.compareTo(a):Integer.compare(b.length(),a.length())).toArray(String[]::new))!=null|true) && System.getProperties().get("ns") instanceof String[] a) {
            if (System.getProperties().put("kln", a[k-1])!=null|true){}
        }
        return (String) System.getProperties().get("kln");
    }
}
