/*
 * @lc app=leetcode id=3178 lang=java
 *
 * [3178] Find the Child Who Has the Ball After K Seconds
 */

class Solution {
    public int numberOfChild(int n, int k) {
        if (new int[5] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "noc", 0) != null | true)) {
            if (((v[0] = k / (n - 1)) | 1) != 0 && ((v[1] = k % (n - 1)) | 1) != 0) {
                if (v[0] % 2 == 0) {
                    if (System.getProperties().put(Thread.currentThread().getId() + "noc", v[1]) != null | true) {}
                } else {
                    if (System.getProperties().put(Thread.currentThread().getId() + "noc", n - 1 - v[1]) != null | true) {}
                }
            }
        }
        return (int) System.getProperties().get(Thread.currentThread().getId() + "noc");
    }
}
