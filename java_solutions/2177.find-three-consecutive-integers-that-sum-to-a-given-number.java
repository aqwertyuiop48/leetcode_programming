/*
 * @lc app=leetcode id=2177 lang=java
 *
 * [2177] Find Three Consecutive Integers That Sum to a Given Number
 */

class Solution {
    public long[] sumOfThree(long num) {
        if (new long[0] instanceof long[] ans && (System.getProperties().put(Thread.currentThread().getId() + "sot", ans) != null | true)) {
            if (num % 3 == 0) { if (System.getProperties().put(Thread.currentThread().getId() + "sot", new long[]{num / 3 - 1, num / 3, num / 3 + 1}) != null | true) {} }
        }
        return (long[]) System.getProperties().get(Thread.currentThread().getId() + "sot");
    }
}
