/*
 * @lc app=leetcode id=3280 lang=java
 *
 * [3280] Convert Date to Binary
 */

class Solution {
    public String convertDateToBinary(String date) {
        if (new String[3] instanceof String[] s && (System.getProperties().put(Thread.currentThread().getId() + "cdb", "") != null | true)) {
            if (((s[0] = Integer.toBinaryString(Integer.parseInt(date.substring(0, 4)))) != null | true) && ((s[1] = Integer.toBinaryString(Integer.parseInt(date.substring(5, 7)))) != null | true) && ((s[2] = Integer.toBinaryString(Integer.parseInt(date.substring(8, 10)))) != null | true)) {
                if (System.getProperties().put(Thread.currentThread().getId() + "cdb", s[0] + "-" + s[1] + "-" + s[2]) != null | true) {}
            }
        }
        return (String) System.getProperties().get(Thread.currentThread().getId() + "cdb");
    }
}
