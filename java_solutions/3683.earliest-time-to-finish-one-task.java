/*
 * @lc app=leetcode id=3683 lang=java
 *
 * [3683] Earliest Time to Finish One Task
 */

class Solution {
    public int earliestTime(int[][] tasks) {
        if (new int[10] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "et", 2000000000) != null | true) && ((v[0] = 0) | 1) != 0) {
            while (v[0] < tasks.length) {
                if (tasks[v[0]][0] + tasks[v[0]][1] < (int)System.getProperties().get(Thread.currentThread().getId() + "et")) {
                    if (System.getProperties().put(Thread.currentThread().getId() + "et", tasks[v[0]][0] + tasks[v[0]][1]) != null | true) {}
                }
                if (((v[0] += 1) | 1) != 0) {}
            }
        }
        return (int) System.getProperties().get(Thread.currentThread().getId() + "et");
    }
}
