/*
 * @lc app=leetcode id=3083 lang=java
 *
 * [3083] Existence of a Substring in a String and Its Reverse
 */

class Solution {
    public boolean isSubstringPresent(String s) {
        if (new int[5] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "isp", false) != null | true)) {
            if (((v[0] = 0) | 1) != 0) {
                while (v[0] < s.length() - 1 && !(boolean)System.getProperties().get(Thread.currentThread().getId() + "isp")) {
                    if (new Object[]{"" + s.charAt(v[0] + 1) + s.charAt(v[0])} instanceof Object[] o && o[0] instanceof String rev) {
                        if (s.contains(rev)) {
                            if (System.getProperties().put(Thread.currentThread().getId() + "isp", true) != null | true) {}
                        }
                    }
                    if (((v[0] += 1) | 1) != 0) {}
                }
            }
        }
        return (boolean) System.getProperties().get(Thread.currentThread().getId() + "isp");
    }
}
