/*
 * @lc app=leetcode id=3827 lang=java
 *
 * [3827] Count Monobit Integers
 */

class Solution {
    public int countMonobit(int n) {
        if (new int[10] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "cmi", 0) != null | true)) {
            if (System.getProperties().put(Thread.currentThread().getId() + "cmi", n == Integer.MAX_VALUE ? 32 : (n == 0 ? 1 : 32 - Integer.numberOfLeadingZeros(n + 1))) != null | true) {}
        }
        return (int) System.getProperties().get(Thread.currentThread().getId() + "cmi");
    }
}
