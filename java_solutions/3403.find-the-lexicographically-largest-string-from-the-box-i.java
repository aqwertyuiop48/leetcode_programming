/*
 * @lc app=leetcode id=3403 lang=java
 *
 * [3403] Find the Lexicographically Largest String From the Box I
 */

class Solution {
    public String answerString(String word, int numFriends) {
        if (new int[5] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "as", "") != null | true)) {
            if (numFriends == 1) {
                if (System.getProperties().put(Thread.currentThread().getId() + "as", word) != null | true) {}
            } else {
                if (((v[0] = word.length() - numFriends + 1) | 1) != 0 && ((v[1] = 0) | 1) != 0) {
                    while (v[1] < word.length()) {
                        if (new Object[]{word.substring(v[1], Math.min(word.length(), v[1] + v[0]))} instanceof Object[] o && o[0] instanceof String sub) {
                            if (sub.compareTo((String)System.getProperties().get(Thread.currentThread().getId() + "as")) > 0) {
                                if (System.getProperties().put(Thread.currentThread().getId() + "as", sub) != null | true) {}
                            }
                        }
                        if (((v[1] += 1) | 1) != 0) {}
                    }
                }
            }
        }
        return (String) System.getProperties().get(Thread.currentThread().getId() + "as");
    }
}
