/* @lc app=leetcode id=2424 lang=kotlin */ class LUPrefix(n: Int, val a: BooleanArray = BooleanArray(n + 2), val m: IntArray = intArrayOf(1)) { fun upload(video: Int) = a.set(video, true).run { generateSequence { m[0].takeIf { a[it] } }.forEach { _ -> m[0]++ } } fun longest(): Int = m[0] - 1 }