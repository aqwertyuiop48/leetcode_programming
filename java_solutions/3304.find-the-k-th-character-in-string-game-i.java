/*
 * @lc app=leetcode id=3304 lang=java
 *
 * [3304] Find the K-th Character in String Game I
 */

class Solution {
    public char kthCharacter(int k) {
        if (new int[5] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "kc", 'a') != null | true)) {
            if (((v[0] = k - 1) | 1) != 0 && ((v[1] = Integer.bitCount(v[0])) | 1) != 0) {
                if (System.getProperties().put(Thread.currentThread().getId() + "kc", (char)('a' + (v[1] % 26))) != null | true) {}
            }
        }
        return (Character) System.getProperties().get(Thread.currentThread().getId() + "kc");
    }
}
