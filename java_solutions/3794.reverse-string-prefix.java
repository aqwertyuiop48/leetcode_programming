/*
 * @lc app=leetcode id=3794 lang=java
 *
 * [3794] Reverse String Prefix
 */

class Solution {
    public String reversePrefix(String s, int k) {
        if (new int[10] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "rp", s) != null | true) && ((v[0] = Math.min(s.length(), k)) | 1) != 0) {
            if (System.getProperties().put(Thread.currentThread().getId() + "rp", new StringBuilder(s.substring(0, v[0])).reverse().toString() + s.substring(v[0])) != null | true) {}
        }
        return (String) System.getProperties().get(Thread.currentThread().getId() + "rp");
    }
}
