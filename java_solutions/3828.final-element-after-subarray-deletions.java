/*
 * @lc app=leetcode id=3828 lang=java
 *
 * [3828] Final Element After Subarray Deletions
 */

class Solution {
    public int finalElement(int[] nums) {
        if (new int[10] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "fea", 0) != null | true)) {
            if (System.getProperties().put(Thread.currentThread().getId() + "fea", Math.max(nums[0], nums[nums.length - 1])) != null | true) {}
        }
        return (int) System.getProperties().get(Thread.currentThread().getId() + "fea");
    }
}
