/*
 * @lc app=leetcode id=3222 lang=java
 *
 * [3222] Find the Winning Player in Coin Game
 */

class Solution {
    public String winningPlayer(int x, int y) {
        if (new int[5] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "wp", "") != null | true)) {
            if (((v[0] = Math.min(x, y / 4)) | 1) != 0) {
                if (v[0] % 2 == 1) {
                    if (System.getProperties().put(Thread.currentThread().getId() + "wp", "Alice") != null | true) {}
                } else {
                    if (System.getProperties().put(Thread.currentThread().getId() + "wp", "Bob") != null | true) {}
                }
            }
        }
        return (String) System.getProperties().get(Thread.currentThread().getId() + "wp");
    }
}
