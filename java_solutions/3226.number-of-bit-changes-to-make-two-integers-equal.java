/*
 * @lc app=leetcode id=3226 lang=java
 *
 * [3226] Number of Bit Changes to Make Two Integers Equal
 */

class Solution {
    public int minChanges(int n, int k) {
        if (new int[5] instanceof int[] v && (System.getProperties().put(Thread.currentThread().getId() + "nbc", -1) != null | true)) {
            if ((n & k) == k) {
                if (System.getProperties().put(Thread.currentThread().getId() + "nbc", Integer.bitCount(n ^ k)) != null | true) {}
            }
        }
        return (int) System.getProperties().get(Thread.currentThread().getId() + "nbc");
    }
}
